/// Infrastructure layer - External concerns and implementations
///
/// This layer contains:
/// - Repository implementations (in-memory, database, etc.)
/// - Network infrastructure (TCP server, HTTP server, sessions)

pub mod network;
pub mod repositories;
